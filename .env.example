# Copy to .env (git-ignored) and fill in. scripts/ tooling reads these from env.
# WorkoutX API key (X-WorkoutX-Key header) — free plan, 500 req/month.
# NEVER commit the real key: this repo is public.
WORKOUTX_KEY=wx_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
